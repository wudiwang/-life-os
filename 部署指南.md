# 部署指南（约 15 分钟，从零到上线）

> 流程与领航项目完全一致：Supabase 建库 → GitHub 建仓 → Vercel 导入 → git push 即上线。

## 第 1 步：Supabase（约 5 分钟）

1. 打开 https://supabase.com → New project（区域建议 Singapore，离国内近）；
2. 项目建好后，左侧 **SQL Editor** → New query → 把本项目 `supabase/v1_init.sql` 的内容全部粘贴进去 → **Run**；
   - 看到 "Success. No rows returned" 即成功（18 张表全部建好，RLS 已关闭）；
3. 左侧 **Storage** → New bucket → 名称填 `attachments` → 勾选 **Public bucket** → Save；
   （体检报告、照片附件存这里）
4. 左侧 **Settings → API**，记下两个值：
   - `Project URL`（形如 `https://xxxx.supabase.co`）
   - `anon public` key

## 第 2 步：本地验证（约 2 分钟）

```bash
# 项目根目录
copy .env.example .env
# 编辑 .env，填入上一步的两个值：
#   VITE_SUPABASE_URL=https://xxxx.supabase.co
#   VITE_SUPABASE_ANON_KEY=eyJhbGci......

npm install
npm run dev
```

打开 http://localhost:5173 —— 左下角显示 **🟢 已连接 Supabase** 即成功。随便录一条数据，去 Supabase 的 Table Editor 里能看到就是通的。

## 第 3 步：GitHub + Vercel（约 5 分钟）

```bash
# GitHub 上新建私有仓库 life-os（不要初始化 README）
git remote add origin https://github.com/wudiwang/life-os.git
git push -u origin main
```

1. 打开 https://vercel.com → Add New → Project → 导入 `life-os` 仓库；
2. Framework 自动识别为 Vite，无需改动；
3. **Environment Variables** 添加 3 个：

| 变量名 | 值 | 用途 |
|---|---|---|
| `VITE_SUPABASE_URL` | 第 1 步的 Project URL | 前端连数据库 |
| `VITE_SUPABASE_ANON_KEY` | 第 1 步的 anon key | 前端连数据库 |
| `CLAUDE_API_KEY` | 你的 Claude API key | AI 分析（服务端用，不会暴露给前端） |

4. Deploy，约 1-2 分钟后拿到 `https://xxx.vercel.app`。

> Claude API key 在 https://platform.claude.com → API Keys 创建。不填也行，只是 AI 分析页不可用，其他功能不受影响。

## 第 4 步：验收清单

- [ ] 左下角显示 🟢 已连接 Supabase
- [ ] 每日一记：写一篇今天的记录
- [ ] 健康：记一条体重，趋势图出现数据点
- [ ] 健康 → 体检报告：上传一个 PDF，能打开附件链接
- [ ] 习惯：建一个习惯并打卡
- [ ] 目标：建一个月度目标 + 一个检查点
- [ ] AI 分析：选"综合分析"→ 开始分析，能返回结果
- [ ] 设置：导出备份 JSON 正常下载

## 日常发布链路（同领航）

```
改代码 → git add → git commit → git push origin main → Vercel 自动构建发布（约 1-2 分钟）
```

## 以后加新表 / 改表

写 `supabase/v2_xxx.sql`（版本递增），在 Supabase SQL Editor 手动执行。新表记得 `disable row level security`。

## 常见问题

- **AI 分析报"接口不可用"**：本地 `npm run dev` 没有 Serverless 环境，AI 功能需在 Vercel 部署后使用（或本地 `npx vercel dev`）；
- **上传附件失败**：检查 Storage bucket 名称必须是 `attachments` 且为 Public；
- **数据没进云端**：检查 `.env`（本地）或 Vercel 环境变量是否填对，改完 Vercel 环境变量需要 Redeploy。
